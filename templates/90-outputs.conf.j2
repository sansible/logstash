##
# {{ ansible_managed }}
#

output {
	if [index_name] {
		elasticsearch {
			flush_size => '100'
			hosts => [ '{{ elk_logstash.outputs.elasticsearch.host }}:{{ elk_logstash.outputs.elasticsearch.port }}' ]
			index => 'logstash-%{+YYYY.MM.dd}-%{index_name}'
		}
	} else {
		elasticsearch {
			flush_size => '100'
			hosts => [ '{{ elk_logstash.outputs.elasticsearch.host }}:{{ elk_logstash.outputs.elasticsearch.port }}' ]
			index => 'logstash-%{+YYYY.MM.dd}'
		}
	}
}
