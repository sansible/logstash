##
# {{ ansible_managed }}
#

output {
	elasticsearch {
		flush_size => '100'
		hosts => [ '{{ sansible_logstash_outputs_elasticsearch_host }}:{{ sansible_logstash_outputs_elasticsearch_port }}' ]
		index => 'logstash-%{+YYYY.MM.dd}'
	}
}
